<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:GuideApp"
             x:Class="GuideApp.ArticlePage"
             Title="{Binding Title}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ScrollView Grid.Row="0" Grid.RowSpan="6">
            <VerticalStackLayout HorizontalOptions="Center">
                <Label Text="{Binding Title}" FontSize="24" FontAttributes="Bold" />

                <CollectionView ItemsSource="{Binding Content}">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <VerticalStackLayout>
                                <!-- Текст -->
                                <Label 
                                    FontSize="18" 
                                    Text="{Binding Content}" 
                                    IsVisible="{Binding IsImage, Converter={StaticResource InverseBoolConverter}}" />
                               
                                <!-- Картинка -->
                                <Image 
                                    Source="{Binding Content}" 
                                    IsVisible="{Binding IsImage}" 
                                    HeightRequest="200" />
                            </VerticalStackLayout>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            </VerticalStackLayout>
        </ScrollView>
        <controls:MainPanel Background="MintCream" Grid.Row="6"/>
    </Grid>
</ContentPage>